<odoo>
    <!-- Vista principal heredada -->
    <record id="view_move_form_inherit_alert" model="ir.ui.view">
        <field name="name">account.move.form.custom.alert</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Mensaje estático en el formulario -->
            <xpath expr="//header" position="after">
                <div class="alert alert-danger" attrs="{'invisible': [('state', '!=', 'draft')]}" style="
                    background: #f8d7da;
                    color: #721c24;
                    padding: 12px;
                    margin: 15px;
                    border-radius: 4px;
                    display: flex;
                    align-items: center;
                    border-left: 4px solid #dc3545;">
                    <i class="fa fa-exclamation-triangle" style="margin-right: 10px;"/>
                    <div>
                        <strong>AVISO: PERIODO ANTERIOR SIN CERRAR</strong><br/>
                                    Antes de iniciar un nuevo periodo, debe cerrarse correctamente el periodo anterior.
                                    Por favor, revise y complete el cierre correspondiente para evitar inconsistencias en los registros.<br/>
                        <strong>⚠️ Esto puede constituir un ilícito fiscal.</strong>
                    </div>
                </div>
            </xpath>
        </field>
    </record>

    <!-- Vista especial para el diálogo de advertencia -->
    <record id="view_move_warning_form" model="ir.ui.view">
        <field name="name">account.move.warning.form</field>
        <field name="model">account.move</field>
        <field name="arch" type="xml">
            <form string="Alerta de Periodo">
                <sheet>
                    <div class="alert alert-danger" style="
                        padding: 16px;
                        margin-bottom: 20px;
                        border-radius: 4px;
                        background-color: #f8d7da;
                        color: #721c24;
                        border-left: 4px solid #dc3545;">
                        <i class="fa fa-exclamation-triangle mr-2" style="font-size: 1.5em;"/>
                        <h3>¡ADVERTENCIA IMPORTANTE!</h3>
                        <div t-field="warning_message" t-options="{'widget': 'html'}"/>
                    </div>
                    
                    <footer>
                        <button name="action_post" string="Continuar de Todos Modos" type="object" class="btn-danger"/>
                        <button string="Cancelar" class="btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>
</odoo>