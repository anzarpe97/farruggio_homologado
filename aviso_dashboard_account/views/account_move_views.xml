<odoo>
    <record id="view_move_form_inherit_alert" model="ir.ui.view">
        <field name="name">account.move.form.custom.alert</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Insertar en el header sin condiciones -->
            <xpath expr="//header" position="after">
                <div class="oe_notification" style="
                    background: #f8d7da;
                    color: #721c24;
                    padding: 12px;
                    margin: 15px;
                    border-radius: 4px;
                    display: flex;
                    align-items: center;">
                    <i class="fa fa-exclamation-circle" style="margin-right: 10px;"/>
                    <strong>AVISO: </strong> 🔔 ADVERTENCIA: PERIODO ANTERIOR SIN CERRAR
                    Antes de iniciar un nuevo periodo, debe cerrarse correctamente el periodo anterior.
                    Por favor, revise y complete el cierre correspondiente para evitar inconsistencias en los registros.
                    <strong>Si no Estaria Cometiendo ilícito Fiscal</strong>    
                </div>
            </xpath>
        </field>
    </record>
</odoo>