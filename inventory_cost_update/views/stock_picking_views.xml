<odoo>
    <!-- <record id="view_picking_form_inherited_cost" model="ir.ui.view">
        <field name="name">stock.picking.form.cost.usd</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml"> -->
            <!-- Insertamos después del campo scheduled_date, que sí existe -->
            <!-- <xpath expr="//field[@name='scheduled_date']" position="after">
                <field name="custom_cost_usd" attrs="{'invisible': [('picking_type_code', '!=', 'incoming')]}"/>
            </xpath>
        </field>
    </record> -->
</odoo>
