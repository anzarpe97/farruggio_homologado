<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data>
    <!-- Reemplaza el header completo en la vista de Pedidos de Venta -->
    <record id="sale_order_form_custom_header" model="ir.ui.view">
      <field name="name">sale.order.form.custom.header</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form"/>
      <field name="arch" type="xml">
        <xpath expr="//form/header" position="replace">
          <header>
            <field name="authorized_transaction_ids" invisible="1"/>
            <button name="payment_action_capture" type="object" data-hotkey="shift+g" string="Capture Transaction" class="oe_highlight" attrs="{'invisible': [('authorized_transaction_ids', '=', [])]}"/>
            <button name="payment_action_void" type="object" string="Void Transaction" data-hotkey="shift+v" confirm="Are you sure you want to void the authorized transaction? This action can't be undone." attrs="{'invisible': [('authorized_transaction_ids', '=', [])]}"/>
            <button id="create_invoice" name="491" string="Crear Factura" invisible="0" type="action" class="btn-primary" data-hotkey="q" attrs="{'invisible': [('state', 'not in', ['sale'])]}}"/>
            <button id="create_invoice_percentage" invisible="1" name="491" string="Crear Factura" type="action" context="{'default_advance_payment_method': 'percentage'}" data-hotkey="q" attrs="{}"/>
            <button name="action_quotation_send" invisible="1" string="ENVIAR PRESUPUESTO POR CORREO" type="object"  class="btn-primary" data-hotkey="g" context="{'validate_analytic': True}"/>
            <button name="action_quotation_send" type="object" string="Send PRO-FORMA Invoice" groups="sale.group_proforma_sales" class="btn-primary" attrs="{'invisible': [('state', 'in', ['sale'])]}" invisible="1" context="{'proforma': True, 'validate_analytic': True}"/>
            <button name="action_confirm" id="action_confirm" data-hotkey="v" string="Confirmar" class="btn-primary" type="object" context="{'validate_analytic': True}" attrs="{'invisible': [('state', 'in', ['sale'])]}" invisible="0"/>
            <button name="action_confirm" data-hotkey="v" string="Confirm" type="object" context="{'validate_analytic': True}" invisible="1" attrs="{}"/>
            <button name="action_quotation_send" type="object" string="Send PRO-FORMA Invoice" groups="sale.group_proforma_sales" attrs="{}" invisible="1" context="{'proforma': True, 'validate_analytic': True}"/>
            <button name="action_quotation_send" string="Send by Email" type="object" invisible="1" data-hotkey="g" context="{'validate_analytic': True}"/>
            <button name="action_cancel" type="object" string="Cancelar" attrs="{'invisible': [('state', 'in', ['sale'])]}" data-hotkey="z"/>
            <button name="action_draft" states="Reestablecer a Borrador" type="object" string="Set to Quotation" data-hotkey="w"/>
            <field name="state" widget="statusbar" statusbar_visible="draft,sent,sale"/>
          </header>
        </xpath>
      </field>
    </record>
  </data>
</odoo>
